const router = require('express').Router();
const Routes = require('');
const Routes = require('');
const Routes = require('');

router.use('/', Routes);
router.use('/', Routes);
router.use('/', Routes);

module.exports = router;